// colors.ts

// Define the shape of the colors object
export type Colors = {
  orange: string;
  lightGray: string;
  green: string;
};

// Define the colors object with the appropriate types
const colors: Colors = {
  orange: '#ff643f',
  lightGray: '#ecf0f1',
  green: '#57b589',
};

export default colors;
